# 飞书通知格式修复说明

## 问题描述

飞书收到的消息显示为原始JSON格式：
```
{"content":{"text":"🔔 任务跟踪表填写提醒\n\n..."}}
```

而不是格式化的正常消息。

## 问题原因

飞书有两种webhook类型，它们接收的消息格式不同：

### 1. **自定义机器人webhook**（推荐）✅
- URL格式: `https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxxx`
- 支持：文本消息、卡片消息、富文本消息
- 消息格式标准，支持Markdown和交互式卡片

### 2. **工作流webhook**（当前使用）⚠️
- URL格式: `https://your-domain.feishu.cn/flow/api/trigger-webhook/xxxxxxxxx`
- 需要：在飞书工作流中自定义解析逻辑
- 消息格式取决于工作流配置

## 解决方案（3个选择）

### 方案1：使用飞书自定义机器人（推荐）⭐⭐⭐⭐⭐

**步骤：**

1. **在飞书群组中添加自定义机器人**
   - 打开要接收通知的飞书群聊
   - 点击右上角设置 → 群机器人 → 添加机器人
   - 选择"自定义机器人"
   - 设置机器人名称：`测试任务跟踪系统`
   - 设置机器人描述（可选）
   - 安全设置选择"签名校验"（推荐）或不设置
   - 复制 webhook地址

2. **在系统中更新配置**
   - 登录系统
   - 进入"Webhook配置"页面
   - 在飞书配置区域，粘贴新的webhook地址
   - 格式应该类似：`https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxx`
   - 如果设置了签名校验，填入签名密钥
   - 点击"测试通知"验证

3. **优点**
   - ✅ 消息格式自动解析，无需额外配置
   - ✅ 支持富文本卡片，显示效果更好
   - ✅ 支持Markdown格式
   - ✅ 可以@群成员
   - ✅ 代码已完全支持，无需修改

### 方案2：修改飞书工作流配置（中等难度）⭐⭐⭐

如果必须使用工作流webhook：

1. **在飞书工作流中配置数据解析**
   - 进入您的飞书工作流编辑页面
   - 在触发节点后添加"数据处理"节点
   - 提取字段配置：
     ```
     title: $.title 或 $.content.title
     content: $.content 或 $.text
     time: $.time
     ```

2. **配置消息发送节点**
   - 添加"发送群消息"节点
   - 消息类型选择"文本"或"富文本"
   - 消息内容使用上一步提取的字段：
     ```
     {{title}}
     
     {{content}}
     
     时间：{{time}}
     ```

3. **更新后端代码（我已经完成）**
   - 我已修改 `buildWorkflowMessage` 方法
   - 现在发送多种格式兼容的数据
   - 重新部署后端应用

### 方案3：两者都配置（最佳实践）⭐⭐⭐⭐⭐

1. **创建飞书自定义机器人用于实时告警**
   - 任务分配、超时告警等紧急通知
   - 配置到系统的飞书webhook

2. **保留工作流webhook用于复杂处理**
   - 如果需要根据通知类型路由到不同群组
   - 如果需要对消息进行复杂处理或存档

## 修复内容（已完成）

我已经修改了 `FeiShuNotificationService.java` 中的 `buildWorkflowMessage` 方法：

```java
private Map<String, Object> buildWorkflowMessage(Notification notification) {
    Map<String, Object> message = new HashMap<>();
    
    // 提供3种兼容格式
    // 格式1: 直接text字段
    message.put("text", fullText);
    
    // 格式2: 分离的title和content
    message.put("title", notification.getTitle());
    message.put("content", cleanContent);
    
    // 格式3: 标准msg_type格式
    message.put("msg_type", "text");
    message.put("content", contentObj);
    
    // 附加元数据
    message.put("notification_type", ...);
    message.put("priority", ...);
    message.put("time", ...);
    
    return message;
}
```

## 验证步骤

### 如果使用方案1（机器人webhook）：

1. 重新部署后端应用
2. 在系统中配置机器人webhook地址
3. 点击"测试通知"按钮
4. 检查飞书群组是否收到格式良好的卡片消息

### 如果使用方案2（工作流webhook）：

1. 配置飞书工作流的数据解析
2. 重新部署后端应用
3. 触发一个测试任务跟踪提醒
4. 检查后端日志查看发送的JSON格式
5. 检查飞书群组是否收到格式化消息

## 推荐配置示例

### 飞书自定义机器人设置

```
机器人名称：测试任务跟踪系统
机器人描述：自动发送任务分配、超时提醒等通知
安全设置：签名校验（可选）
消息格式：交互式卡片（自动）
```

### 系统配置

```
飞书通知：启用 ✓
Webhook地址：https://open.feishu.cn/open-apis/bot/v2/hook/xxxxx-xxxxx-xxxxx
签名密钥：SEC1234567890abcdef（如果启用签名校验）
```

## 常见问题

### Q1: 如何判断我当前使用的是哪种webhook？

查看webhook地址：
- 包含 `/open-apis/bot/` → 机器人webhook ✅
- 包含 `/flow/api/trigger-webhook/` → 工作流webhook ⚠️

### Q2: 可以同时配置多个飞书群接收通知吗？

可以，有两种方式：
1. 将机器人添加到多个群组（同一个机器人）
2. 配置多个webhook地址（需要修改代码支持）

### Q3: 为什么推荐使用机器人webhook？

- 官方标准格式，兼容性好
- 消息显示效果更好（支持卡片）
- 配置简单，无需额外设置
- 代码已完全支持

## 部署步骤

1. **重新编译后端**
   ```bash
   mvn clean package -DskipTests
   ```

2. **重启应用**
   ```bash
   docker-compose restart backend
   ```

3. **更新配置**
   - 登录系统
   - 进入"Webhook配置"
   - 更新飞书webhook地址（改为机器人webhook）
   - 测试通知

## 需要帮助？

如果您希望：
1. 继续使用工作流webhook - 我可以帮您配置飞书工作流
2. 改用机器人webhook - 我可以指导您创建和配置机器人
3. 自定义消息格式 - 我可以修改代码满足您的需求

请告诉我您的选择！


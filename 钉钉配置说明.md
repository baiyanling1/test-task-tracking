# 钉钉配置说明

## 配置步骤

### 1. 在钉钉群中添加自定义机器人

1. 打开钉钉群聊
2. 点击群设置 → 智能群助手 → 添加机器人 → 自定义
3. 设置机器人名称和头像
4. 配置安全设置（推荐使用签名方式）
5. 获取webhook地址

### 2. 配置安全设置

#### 方式一：关键词
- 在安全设置中选择"关键词"
- 添加关键词，如：`测试任务跟踪系统`

#### 方式二：签名（推荐）
- 在安全设置中选择"签名"
- 记录下签名密钥

### 3. 修改application.yml配置

```yaml
# 钉钉通知配置
dingtalk:
  enabled: true  # 启用钉钉通知
  webhook:
    url: https://oapi.dingtalk.com/robot/send?access_token=你的access_token  # 替换为实际的webhook地址
  secret: 你的签名密钥  # 如果使用签名方式，填入签名密钥
```

### 4. 测试配置

1. 重启应用
2. 访问 `/dingtalk-test` 页面
3. 点击"发送测试消息"按钮
4. 检查钉钉群是否收到消息

## 常见问题

### 1. 400错误
- 检查webhook地址是否正确
- 确认access_token是否有效
- 检查安全设置是否正确

### 2. 签名错误
- 确认签名密钥是否正确
- 检查时间戳是否同步

### 3. 关键词错误
- 确认消息内容包含配置的关键词

## 示例配置

```yaml
dingtalk:
  enabled: true
  webhook:
    url: https://oapi.dingtalk.com/robot/send?access_token=abc123def456
  secret: SEC000000000000000000000
```

## 注意事项

1. access_token是敏感信息，不要泄露
2. 签名密钥要妥善保管
3. 建议使用签名方式，安全性更高
4. 测试时建议先用关键词方式，确认基本功能正常后再切换到签名方式

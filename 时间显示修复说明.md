# 时间显示修复说明

## 问题描述
前端显示的"最后执行时间"和"下次执行时间"不一致：
- 最后执行时间显示为本地时间（正确）
- 下次执行时间显示为UTC时间（错误）

## 问题原因
1. 后端使用 `LocalDateTime` 类型存储时间，这是一个不带时区信息的时间类型
2. 虽然 `application.yml` 中配置了 Jackson 的时区为 `Asia/Shanghai`，但在实际序列化时可能没有正确应用
3. 前端在解析时间字符串时，JavaScript 的 `new Date()` 会将没有时区信息的字符串按照本地浏览器时区解析，导致时间被二次转换

## 解决方案

### 后端修改
在所有 DTO 类的 `LocalDateTime` 字段上添加 `@JsonFormat` 注解，明确指定序列化格式和时区：

```java
@JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss", timezone = "Asia/Shanghai")
private LocalDateTime lastExecuteTime;

@JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss", timezone = "Asia/Shanghai")
private LocalDateTime nextExecuteTime;
```

修改的文件列表：
- `src/main/java/com/testtracking/dto/ScheduledTaskDto.java`
- `src/main/java/com/testtracking/dto/TestTaskDto.java`
- `src/main/java/com/testtracking/dto/ScheduledTaskExecutionDto.java`
- `src/main/java/com/testtracking/dto/NotificationDto.java`
- `src/main/java/com/testtracking/dto/TaskProgressDto.java`
- `src/main/java/com/testtracking/dto/LoginHistoryDto.java`
- `src/main/java/com/testtracking/dto/UserDto.java`
- `src/main/java/com/testtracking/dto/UserUpdateDto.java`
- `src/main/java/com/testtracking/dto/DepartmentDto.java`

### 前端修改
修改 `frontend/src/views/ScheduledTasks.vue` 中的 `formatDateTime` 函数：

**修改前：**
- 解析时间字符串并使用 `toLocaleString` 转换时区
- 可能导致时间被二次转换

**修改后：**
- 直接返回后端传来的 "yyyy-MM-dd HH:mm:ss" 格式字符串
- 避免不必要的时区转换
- 保持兜底处理逻辑以应对其他可能的时间格式

## 技术细节

### 为什么使用 @JsonFormat
`@JsonFormat` 注解告诉 Jackson 序列化器：
1. 使用指定的格式模式（`yyyy-MM-dd HH:mm:ss`）
2. 在指定的时区（`Asia/Shanghai`）中格式化时间
3. 确保所有时间字段的序列化行为一致

### 为什么修改前端函数
后端返回的时间字符串格式为 "2024-11-06 14:30:00"，这是一个没有时区标识的字符串。JavaScript 的 `new Date()` 在解析这种字符串时：
- 如果浏览器时区不是 `Asia/Shanghai`，会错误地按照本地时区解析
- 然后使用 `toLocaleString` 转换到 `Asia/Shanghai`，导致时间被二次转换

通过直接显示后端返回的字符串，避免了这个问题。

## 部署步骤

1. **重新编译后端项目**
   ```bash
   mvn clean package -DskipTests
   ```

2. **重启后端服务**
   ```bash
   # 根据您的部署方式重启服务
   ```

3. **前端无需重新构建**（如果使用开发模式）
   - 保存修改后，Vite 会自动热更新
   
4. **前端重新构建**（如果是生产环境）
   ```bash
   cd frontend
   npm run build
   ```

5. **验证修复**
   - 打开定时任务管理页面
   - 检查"最后执行时间"和"下次执行时间"是否都显示为本地时间（Asia/Shanghai）
   - 两个时间的显示格式应该一致

## 注意事项

1. 所有的时间都统一使用 `Asia/Shanghai` 时区
2. 前端显示的时间就是数据库中存储的时间（Asia/Shanghai 时区）
3. 如果用户在不同时区访问系统，显示的仍然是 Asia/Shanghai 时区的时间
4. 这种处理方式适合所有用户都在同一时区的场景

## 后续优化建议

如果将来需要支持多时区用户，可以考虑：
1. 后端使用 `ZonedDateTime` 或 `OffsetDateTime` 存储时间
2. 前端根据用户的时区设置转换显示时间
3. 数据库存储 UTC 时间，前后端按需转换


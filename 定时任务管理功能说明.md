# 定时任务管理功能说明

## 功能概述

本系统新增了定时任务管理功能，允许管理员查看、管理和手动触发系统中的所有定时任务，包括周五任务跟踪表填写提醒通知功能。

## 新增功能

### 1. 周五任务跟踪表填写提醒通知

**功能描述**：
- 每周一早上10点自动检查上周五未填写或更新任务跟踪表的人员
- 发送钉钉群通知和系统内部告警
- 支持手动触发检查

**检测逻辑**：
- 检查上周五是否为工作日
- 检查每个有分配任务的用户在上周五是否有活动记录
- 活动记录包括：创建新任务、更新任务进度、填写工时等

**通知内容**：
- 钉钉通知：包含未填写人员列表、部门信息、任务数量
- 系统通知：发送给未填写人员的个人提醒

**配置信息**：
- 执行时间：每周一早上9点半 (`0 30 9 * * 1`)
- 检查范围：所有活跃用户
- 通知方式：钉钉群通知 + 系统内部告警

### 2. 定时任务管理界面

**功能特性**：
- 查看所有定时任务列表
- 显示任务执行状态和历史
- 手动触发定时任务
- 启用/禁用定时任务

**系统内置任务**：

| 任务名称 | 描述 | 执行计划 | 状态 |
|---------|------|---------|------|
| 超时任务检查 | 检查并更新超时任务状态 | 每天凌晨1点 | 启用 |
| 登录历史清理 | 清理一个月前的登录记录 | 每天凌晨2点 | 启用 |
| 数据库备份 | 每周一凌晨备份数据库 | 每周一凌晨2点 | 启用 |
| 过期通知清理 | 清理过期通知记录 | 每天凌晨2点 | 启用 |
| 任务跟踪提醒 | 检查周五未填写任务的人员 | 每周一早上9点半 | 启用 |

## 技术实现

### 后端实现

**新增文件**：
- `TaskTrackingReminderService.java` - 任务跟踪提醒服务
- `ScheduledTaskManagementService.java` - 定时任务管理服务
- `ScheduledTaskManagementController.java` - 定时任务管理控制器
- `ScheduledTask.java` - 定时任务实体
- `ScheduledTaskExecution.java` - 任务执行记录实体
- `ScheduledTaskDto.java` - 定时任务DTO
- `ScheduledTaskExecutionDto.java` - 执行记录DTO
- `ScheduledTaskRepository.java` - 定时任务Repository
- `ScheduledTaskExecutionRepository.java` - 执行记录Repository

**数据库表**：
- `scheduled_tasks` - 定时任务表
- `scheduled_task_executions` - 任务执行记录表

### 前端实现

**新增文件**：
- `frontend/src/views/ScheduledTasks.vue` - 定时任务管理页面
- `frontend/src/api/scheduledTasks.js` - 定时任务API接口

**路由配置**：
- 添加 `/scheduled-tasks` 路由
- 仅admin用户可访问

**菜单配置**：
- 在主布局中添加"定时任务管理"菜单项
- 仅admin用户可见

## API接口

### 定时任务管理接口

**获取任务列表**：
```
GET /api/admin/scheduled-tasks
```

**手动触发任务**：
```
POST /api/admin/scheduled-tasks/{taskName}/trigger
```

**获取执行历史**：
```
GET /api/admin/scheduled-tasks/{taskName}/history
```

**切换任务状态**：
```
PUT /api/admin/scheduled-tasks/{taskName}/toggle?enabled={boolean}
```

## 使用说明

### 管理员操作

1. **查看任务列表**：
   - 登录系统后，在左侧菜单点击"定时任务管理"
   - 查看所有定时任务的执行状态和计划

2. **手动触发任务**：
   - 在任务列表中点击"手动执行"按钮
   - 系统会立即执行该任务
   - 执行结果会显示在界面上

3. **查看执行历史**：
   - 点击"执行历史"按钮查看任务的执行记录
   - 包含执行时间、状态、时长、错误信息等

4. **管理任务状态**：
   - 使用开关按钮启用或禁用定时任务
   - 禁用的任务不会自动执行

### 任务跟踪提醒

**自动执行**：
- 系统会在每周一早上9点半自动检查
- 如果发现未填写人员，会自动发送通知

**手动触发**：
- 管理员可以在定时任务管理界面手动触发
- 用于测试或特殊情况下的检查

## 配置说明

### 钉钉通知配置

在 `application.yml` 中配置钉钉webhook：

```yaml
dingtalk:
  enabled: true
  webhook:
    url: https://oapi.dingtalk.com/robot/send?access_token=your_token
  secret: your_secret
```

### 任务执行时间配置

各任务的执行时间可以通过修改cron表达式调整：

- 超时任务检查：`0 0 1 * * ?` (每天凌晨1点)
- 登录历史清理：`0 0 2 * * ?` (每天凌晨2点)
- 数据库备份：`0 0 2 * * 1` (每周一凌晨2点)
- 过期通知清理：`0 0 2 * * ?` (每天凌晨2点)
- 任务跟踪提醒：`0 30 9 * * 1` (每周一早上9点半)

## 注意事项

1. **权限控制**：只有admin用户才能访问定时任务管理功能
2. **任务执行**：手动触发任务时，系统会记录执行历史和结果
3. **错误处理**：任务执行失败时会记录错误信息，便于排查问题
4. **性能考虑**：大量任务同时执行时可能影响系统性能，建议合理设置执行时间
5. **数据备份**：数据库备份任务需要确保服务器有足够的存储空间

## 扩展功能

未来可以考虑添加的功能：

1. **任务执行统计**：统计任务执行成功率、平均执行时间等
2. **任务依赖关系**：设置任务之间的依赖关系，确保执行顺序
3. **动态任务配置**：支持运行时修改任务配置
4. **任务执行监控**：实时监控任务执行状态
5. **邮件通知**：除了钉钉通知，还可以添加邮件通知功能

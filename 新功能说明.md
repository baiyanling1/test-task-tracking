# 未填写任务统计功能说明

## 功能概述
在仪表盘中新增了一个统计项，用于显示在指定时间范围内没有填写任务或更新任务进度的人员。

## 功能特性

### 1. 默认显示
- 默认显示上周（周一到周日）没有填写任务或更新任务进度的人员
- 系统自动计算上周时间范围

### 2. 自定义时间范围
- 可以通过时间选择器自定义查询的开始和结束日期
- 支持任意时间范围的查询

### 3. 统计信息
- **总用户数**: 系统中活跃用户的总数量
- **未活跃用户**: 在指定时间范围内没有任何活动记录的用户数量
- **活跃用户**: 在指定时间范围内有活动记录的用户数量
- **活跃率**: 活跃用户占总用户的百分比

### 4. 详细列表
未活跃用户列表包含以下信息：
- 姓名
- 用户名
- 部门
- 邮箱
- 分配任务数（当前未完成的任务数量）
- 状态（显示为"未活跃"）

## 活动判断标准

系统会检查用户在指定时间范围内是否有以下任何一种活动：

1. **创建任务** - 用户创建了新的测试任务
2. **更新任务进度** - 用户添加了任务进度记录
3. **修改任务** - 用户修改了任务的信息或状态

只要用户在时间范围内有任何一种上述活动，就会被认为是活跃用户。

## 权限控制

- 该功能仅对管理员（ADMIN）和经理（MANAGER）角色开放
- 普通测试员（TESTER）无法访问此统计信息

## API接口

### 1. 获取上周未活跃用户
```
GET /api/dashboard/inactive-users
```

### 2. 获取指定时间范围内未活跃用户
```
GET /api/dashboard/inactive-users/range?startDate=2024-01-01&endDate=2024-01-07
```

参数：
- `startDate`: 开始日期 (格式: YYYY-MM-DD)
- `endDate`: 结束日期 (格式: YYYY-MM-DD)

## 使用说明

1. 登录系统后进入仪表盘页面
2. 找到"未填写任务统计"卡片
3. 默认显示上周的统计数据
4. 如需查询其他时间范围：
   - 选择"自定义"选项
   - 使用日期选择器选择开始和结束日期
   - 系统会自动更新统计结果

## 数据展示

- 统计摘要以卡片形式展示关键数字
- 详细列表以表格形式展示未活跃用户信息
- 如果所有用户都有活动记录，会显示"该时间段内所有用户都有活动记录"
- 底部显示当前查询的时间范围

## 样式设计

- 采用响应式设计，适配不同屏幕尺寸
- 未活跃用户数量使用红色突出显示
- 表格样式美观，易于阅读
- 支持移动端查看

## 技术实现

### 后端
- `DashboardService.getInactiveUsers()` - 核心业务逻辑
- `DashboardController` - API接口控制器
- `TestTaskRepository` - 数据查询方法

### 前端
- `Dashboard.vue` - 界面组件
- `api/tasks.js` - API调用函数
- Element Plus - UI组件库

这个功能可以帮助管理员和经理及时发现不活跃的团队成员，促进任务跟踪的完整性和团队协作效率。
